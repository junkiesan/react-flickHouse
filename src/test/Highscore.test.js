import React from "react";
import { render, unmountComponentAtNode } from "react-dom";
import { act } from "react-dom/test-utils";

import Quizz from '../components/quizz';

describe('highscore', () => {
  test('set a new highscore', () => {
    // const { countdown } = gameOver({ time: 0 })
    // gameOver({ resetButton: true  })
    // expect(countdown).toEqual(0)
  })
  
  test('doesnt change highscore cause current score is lower', () => {

  })
 
  test('highscore is displayed near score when superior to zero', () => {

  })
});